# CpFont
A TUI utility to easily install fonts into every software that uses one.  This tool automates the process of moving font files to the correct software's font directory,  handling permissions and ownership under sudo privileges.
